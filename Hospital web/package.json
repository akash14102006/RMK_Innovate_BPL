{
    "name": "bharat-pulselink-hospital-web",
    "private": true,
    "scripts": {
        "install:all": "npm install --prefix frontend && npm install --prefix backend",
        "dev": "npm run dev --prefix frontend",
        "backend": "npm run start --prefix backend",
        "build": "npm run build --prefix frontend && node -e \"const fs=require('fs'); if(fs.existsSync('frontend/dist')){fs.cpSync('frontend/dist', 'dist', {recursive: true});}\"",
        "preview": "npm run preview --prefix frontend",
        "start": "concurrently \"npm run dev\" \"npm run backend\""
    },
    "devDependencies": {
        "concurrently": "^9.2.1"
    }
}
